interface:
  display_name: "New API Docker Deploy"
  short_description: "Deploy New API with strict AIPDD site identity"
  default_prompt: "Use $new-api-docker-deploy for a first deployment or safe ACR update that preserves New API data, the registered AIPDD site UUID, and site-scoped Key rotation."
