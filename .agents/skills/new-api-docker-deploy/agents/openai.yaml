interface:
  display_name: "New API Docker Deploy"
  short_description: "Deploy New API with Docker Compose over SSH"
  default_prompt: "Use $new-api-docker-deploy for a first deployment or safe ACR update of New API with PostgreSQL, Redis, AIPDD, and an optional in-app domain setting."
